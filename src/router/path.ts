/**
 * 主路由
 */
export enum RootRouterPath {
  IM = '/IM',
  Login = '/login',
  Regiset = '/regiset'
}

/**
 * IM 模型下路由
 */
export enum IMRouterPath {
  Message = '/IM/message',
  firend = '/IM/firend',
  me = '/IM/me'
}
